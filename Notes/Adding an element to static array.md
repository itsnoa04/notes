if we need to add an element to a [[Static Arrays]] we create a copy of the array in a new [[memory address]] with more ( usually double ) the [[Memory]].

[[Time Complexity]] of the process is [[O(n)]]