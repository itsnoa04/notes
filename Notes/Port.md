for multiple programs to listen for new network connections on the same machine without codeing, they pick a port to listen on. A port is an integer between 0 and 2^16

ports 0 - 1023 are reserverd as system ports (well known ports) and shouldn't be used by user